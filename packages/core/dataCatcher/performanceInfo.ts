// import { getObserveFn } from 'core/common';

export class Performance {
  // TYODO
}

// new PerformanceObserver((list, obj) => {
//   console.log(obj);

//   list.getEntries().map(v => {
//     console.log(v);
//   });
// }).observe({
//   entryTypes: ['navigation', 'resource', 'mark', 'measure', 'paint']
// });
